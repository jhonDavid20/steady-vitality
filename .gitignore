# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
.next/
out/
*.tsbuildinfo

# Env files (keep .env.example)
.env
.env.*
!.env.example
.env.railway

# Uploaded files — keep directory structure but ignore actual uploads
apps/api/uploads/avatars/*
!apps/api/uploads/avatars/.gitkeep

# Claude Code — personal overrides (keep shared .claude/settings.json)
.claude/settings.local.json

# Misc
/init-scripts
docker-compose.yml
package-lock.json
postman/
*.log
.DS_Store
