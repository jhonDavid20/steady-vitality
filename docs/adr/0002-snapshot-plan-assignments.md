# Plan assignments preserve their schedule as a snapshot

Each plan assignment copies the plan template's scheduled actions when a coach assigns it. We accept extra rows and explicit versioning so later template edits cannot reinterpret a client's historical completions, while coaches can still reuse and evolve templates for future assignments.
